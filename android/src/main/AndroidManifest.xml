<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.stripe.identity.identity">

    <application
        android:allowBackup="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"> <!-- ✅ Add a compatible theme -->
        
        <activity
            android:name="com.stripe.android.identity.IdentityActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"> <!-- ✅ Explicitly set AppCompat theme -->
        </activity>

    </application>
</manifest>